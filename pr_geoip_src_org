rule "pr_geoip_src_org"
when
  has_field(field : "src_ip")
then
  let src_ip_raw = lookup("geoip-lookup-asn", to_string($message."src_ip"));
  set_field("src_org", lowercase(to_string(src_ip_raw["as_organization"])));
  set_field("src_asn", lowercase(to_string(src_ip_raw["as_number"])));
end
